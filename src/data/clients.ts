/**
 * Client logos. Both lists are empty on purpose.
 *
 * They previously held 11 template placeholder logos from `assets/imgs/brands/`,
 * rendered by `shared/Clients.tsx` under the claim "More than 200+ companies
 * trusted our engineering worldwide". Those were not clients, and the logos were
 * not ours to display. The claim was removed from the homepage and later from
 * /about, and the image files themselves have now been deleted, so **the old paths
 * no longer resolve, do not restore them from git history.**
 *
 * Fill these only with logos of real clients who have agreed to be named. See the
 * "never invent data" rule in AGENTS.md.
 */
const clients: string[] = [];

export default clients;

export const CLIENT_BRANDS: string[] = [];
